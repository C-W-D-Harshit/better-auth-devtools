# @better-auth-devtools/shared

Internal shared utilities for Better Auth DevTools.

This package is currently in alpha and may introduce breaking changes between releases.

This package is published to satisfy the runtime dependency graph for the public packages in this monorepo. It is not intended to be a primary integration surface.
