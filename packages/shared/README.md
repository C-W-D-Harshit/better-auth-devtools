# @better-auth-devtools/shared

Internal workspace package for legacy shared helpers.

This package is no longer part of the intended public npm surface. Install `better-auth-devtools` and import from its subpaths instead.
