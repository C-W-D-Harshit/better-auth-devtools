# @better-auth-devtools/core

Shared types, payloads, endpoint constants, and validation helpers for Better Auth DevTools.

This package is currently in alpha and may introduce breaking changes between releases.

This package is primarily consumed by the sibling workspace packages, but it is published so the dependency graph for `@better-auth-devtools/plugin` and `@better-auth-devtools/react` is valid on npm.
