# @better-auth-devtools/core

Internal workspace package for legacy shared types and helpers.

This package is no longer part of the intended public npm surface. Install `@better-auth-devtools/plugin` and `@better-auth-devtools/react` instead.
